# Adds local Streamlit dashboard + tests + the GIS stack.
# Used for `streamlit run app.py`, regenerating the CDTA GeoJSON, running pytest.

-r requirements.txt

# Streamlit dashboard (app.py + pages/Ranking.py)
streamlit>=1.28

# GIS — only needed when running the data pipeline / building cdta_geo.json.
# Railway reads the pre-rendered data/processed/cdta_geo.json instead.
geopandas>=1.0

# K-means cluster cache (HDF5). Optional, only used by kmeans_with_caching.
h5py>=3.0

# Local-model fallback for embeddings (EMBEDDING_BACKEND=sentence_transformers).
# Pulls torch (~750 MB). Skip in production where OPENAI_API_KEY handles embeddings.
sentence-transformers>=3.0

# Local convenience
tabulate>=0.9
matplotlib>=3.7

# Tests
pytest>=7.0
