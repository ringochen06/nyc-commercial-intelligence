# Railway / FastAPI production set. Minimal: no streamlit, no torch, no geopandas.
# Local Streamlit dev needs requirements-dev.txt as well.

# Core
numpy>=1.24
pandas>=2.0
duckdb>=1.0
scikit-learn>=1.3
plotly>=5.0

# LLM / embeddings (OpenAI for query embedding at runtime)
openai>=1.40
anthropic>=0.39
python-dotenv>=1.0

# FastAPI backend
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.0

# Supabase (used by /api/rank when SUPABASE_URL is set)
supabase>=2.0
